/**
 * The serializable schema-snapshot contract — compatibility re-export: the
 * module lives at `@sisal/core/schema` since the v0.8 extraction. New code
 * should import from `@sisal/core` (or `@sisal/core/schema`) directly.
 *
 * @module
 */

export * from "@sisal/core/schema";
