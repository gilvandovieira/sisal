title: Sisal
description: A JSR-first, Deno-native database toolkit with npm support.
markdown: kramdown
plugins:
  - jekyll-relative-links
relative_links:
  enabled: true
  collections: true
defaults:
  - scope:
      path: ""
    values:
      layout: default
