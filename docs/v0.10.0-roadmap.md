---
title: v0.10.0 Roadmap
---

# Sisal v0.10.0 roadmap — `@sisal/etl` Preview

The investigations ([v0.6](v0.6.0-roadmap.md)) and substrate
([v0.8](v0.8.0-roadmap.md), [v0.9](v0.9.0-roadmap.md)) are done; v0.10 ships the
**first preview of Sisal ETL** as a separate package. The model is deliberately
small: a **job definition + a single-run runner** that pushes heavy work down
into the database. It is **not** a scheduler or worker platform.

## Status legend

| Symbol | Meaning                                                                             |
| ------ | ----------------------------------------------------------------------------------- |
| 🔴     | Not started · 🟡 In progress · 🟢 Done — move to CHANGELOG · ⏸️ deferred past v0.10 |

## Priority summary — security hardening (0.9.0 audit)

The [0.9.0 security-audit refresh](security.md) (2026-07-02) left nine open
findings plus one residual; all are scheduled here as T1–T10. **All ten have now
landed** (🟢) — each finding is resolved in [docs/security.md](security.md) and
pinned by a test (see the CHANGELOG). Two sat on this release's own correctness
spine and are therefore P1 prerequisites of the acceptance criteria below:
**T1** (SEC-008 — without it, the "concurrent runs are serialized by the lock
strategy" criterion is unsatisfiable on the MySQL family) and **T5** (SEC-012 —
the checkpoint the runner ships must not be able to prune ahead of its own
horizon). The ETL feature tasklist — the release headline — is drawn up below as
**T11–T23**.

| ID  | Task                                                                       | Stream                 | Priority | Effort | Status |
| --- | -------------------------------------------------------------------------- | ---------------------- | -------- | ------ | ------ |
| T1  | MySQL-family found-rows fix — make `tryInsert`/lock claims trustworthy     | Security · SEC-008     | P1       | L      | 🟢     |
| T2  | TLS options for the MySQL/MariaDB URL + pool paths                         | Security · SEC-009     | P1       | M      | 🟢     |
| T3  | Sanitize driver-attached error properties (`parameters`/`sql`/config)      | Security · SEC-010     | P1       | M      | 🟢     |
| T4  | Redaction gaps: `encryptionKey`, URL userinfo, `NeonError`, `details.sql`  | Security · SEC-011     | P2       | S      | 🟢     |
| T5  | Checkpoint prune: horizon-first order, loud override, generic fail-closed  | Security · SEC-012     | P1       | S      | 🟢     |
| T6  | Namespace the MySQL migration lock by database                             | Security · SEC-013     | P2       | S      | 🟢     |
| T7  | Digest-pin workflow service/example images; drop `--no-lock` from `perf:*` | Security · SEC-014     | P2       | S      | 🟢     |
| T8  | Tag-publish main-ancestry guard; pre-commit hook `--allow-run=deno`        | Security · SEC-015     | P2       | S      | 🟢     |
| T9  | Core DDL: reject params in portable expressions; validate constraint names | Security · SEC-016     | P2       | S      | 🟢     |
| T10 | Migration splitter: `E'…'` escape strings + nested block comments          | Security · SEC-007 res | P2       | S      | 🟢     |

Each task's full analysis, failure scenario, and fix direction live in the
finding's section of [docs/security.md](security.md); when a task lands, move
its finding to **Addressed** there and check off its follow-up item.

- **T1 (SEC-008, High).** Connect with found-rows off (mysql2
  `flags: ["-FOUND_ROWS"]`, MariaDB `foundRows: false`) or move the claim path
  to an unambiguous strategy; audit every `UPDATE` row-count consumer first
  (`AdvisoryLock.renew` flips from "matched" to "changed" semantics); add
  live-conflict integration scenarios (two claimants, one winner) for MySQL and
  MariaDB, plus found-rows-shaped unit fixtures.
- **T2 (SEC-009, Medium).** Add TLS options to `MysqlConnectionOptions`,
  forwarded identically by `createMysqlPool`, `createMariadbPool`, and the
  migrate driver; stop silently dropping `ssl-mode` URL params (forward or
  reject); document strict-TLS examples.
- **T3 (SEC-010, Medium).** Sanitize enumerable properties on preserved driver
  causes (drop/summarize `parameters`, `sql`, `config`; recurse nested causes
  and `AggregateError`); disable value-echoing driver options by default; pin
  with adapter-shaped error fixtures (mysql2, MariaDB, postgres.js, libSQL,
  Neon).
- **T4 (SEC-011, Low).** Broaden `redactSecrets` (`encryptionKey`, reserved
  characters in URL userinfo); route `NeonError` through `SisalError`; redact
  `MigrationError.details.sql`.
- **T5 (SEC-012, Low).** Advance the retention horizon before deleting in
  `prune`; log when `unsafeAllowPrunedReplay` suppresses the guard; fail closed
  on the `generic` dialect.
- **T6 (SEC-013, Low).** `GET_LOCK('sisal:migrate:<database>')` instead of the
  server-global name.
- **T7 (SEC-014, Low).** Digest-pin `mysql:8.4`/`mariadb:11` in
  `integration.yml` `services:` and the example compose images; add a CI check
  rejecting `image:` without `@sha256:`; remove `--no-lock` from the `perf:*`
  tasks.
- **T8 (SEC-015, Low).** Add
  `git merge-base --is-ancestor "$GITHUB_SHA" origin/main` to the tag-push
  publish path; narrow the installed pre-commit hook to `--allow-run=deno`.
- **T9 (SEC-016, Low).** Make `renderPortableExpression` reject expressions
  carrying bound parameters (no dangling `$1` in DDL); validate
  index/unique/check constraint names with the identifier rules.
- **T10 (SEC-007 residual, Low).** Teach `splitSqlStatements` PostgreSQL `E'…'`
  backslash escapes and nested block comments; regression tests.
  Correctness-only (inputs are trusted migration files).

## Priority summary — ETL feature tasks (`@sisal/etl` — the release headline)

The build tasklist for `@sisal/etl`, **T11–T23**, drawn from the
[scope](#scope--initial-features), the
[execution model](#the-execution-model-the-whole-v010-runtime), and the
[acceptance criteria](#acceptance-criteria). Each task **consumes the v0.9
substrate rather than re-inventing it**: the validated checkpoint/watermark
contract (v0.9 T13) and its prune/replay-refusal (v0.9 T14), the portable
advisory-lock abstraction (v0.9 T11) and atomic load+advance (v0.9 T12), and the
`tryInsert`/`WriteOutcome` claim signal (v0.9 T15) — the last of which only
becomes trustworthy on the MySQL family once **T1 (SEC-008)** lands, which is
why the runner is Postgres-first. **All thirteen have now landed** (🟢) — the
package, job model, rollup SQL, checkpoint consumption, window math, runner,
backfill/replay/status/dry-run APIs, capability gate, scheduler docs, and the
Postgres-verified integration suite; see the CHANGELOG.

| ID  | Task                                                                                                                                                     | Stream               | Priority | Effort | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | -------- | ------ | ------ |
| T11 | `@sisal/etl` package scaffold — core-only dep, injectable executor, subpath exports, boundary lint (no `@sisal/orm` edge)                                | ETL · package        | P1       | M      | 🟢     |
| T12 | `defineJob()` — typed job model: source, target, grain, window column, transform/aggregates (inferred from the v0.8 IR + snapshot)                       | ETL · job model      | P1       | L      | 🟢     |
| T13 | `rollup()` generated pushdown SQL — insert-from-select + date bucket + filtered aggregates + upsert-from-select                                          | ETL · rollup SQL     | P1       | L      | 🟢     |
| T14 | Checkpoint store — consume the v0.9-validated `sisal_etl_checkpoints` contract (read / advance / prune)                                                  | ETL · checkpoint     | P1       | M      | 🟢     |
| T15 | Next-window computation — half-open `[from, until)`, `<= now` discipline, bucket-edge off-by-one invariants                                              | ETL · runner         | P1       | M      | 🟢     |
| T16 | `run(job)` — single-window runner: acquire lock (v0.9 T11) → pushdown SQL → atomic checkpoint advance (v0.9 T12); resolve the txn-boundary open question | ETL · runner         | P1       | L      | 🟢     |
| T17 | `backfill(job, range)` — deterministic historical range over successive windows                                                                          | ETL · API            | P1       | M      | 🟢     |
| T18 | `replay(job, window)` — idempotent re-run; refuses windows behind `pruned_before` via v0.9 T14 (`ORM_REPLAY_PRUNED`) + `unsafe` override                 | ETL · API            | P1       | M      | 🟢     |
| T19 | `status(job)` — report checkpoint + last-run state                                                                                                       | ETL · API            | P2       | S      | 🟢     |
| T20 | `dry-run` / `explain` — print generated SQL without executing; golden-SQL snapshot per dialect                                                           | ETL · API            | P1       | M      | 🟢     |
| T21 | Capability-gating — Postgres-first; other engines expose the supported subset or return a typed "unsupported job shape," never silently degrade          | ETL · dialects       | P1       | M      | 🟢     |
| T22 | External-scheduler docs — cron / systemd-timer / GitHub-Actions / `pg_cron` (never the sole path)                                                        | ETL · docs           | P2       | S      | 🟢     |
| T23 | `post_events → post_hourly_stats` example + gated integration suite (idempotency / resume / backfill / lock-contention)                                  | ETL · examples/tests | P1       | L      | 🟢     |

**P1 spine (the acceptance-criteria critical path).** T11→T16 build the package,
job model, rollup SQL, checkpoint, window math, and the single-window runner —
the minimum that makes `defineJob` + `run` execute a pushed-down rollup. T17
(backfill), T18 (idempotent replay), T20 (dry-run verification), T21
(capability-gating), and T23 (the Postgres example + gated integration suite)
each discharge a **named acceptance criterion**. T19 (status) and T22 (scheduler
docs) are required for the release but sit off the correctness spine. Dependency
order: **T11 → T12 → T13 → T14 → T15 → T16**, then T17/T18/T19/T20 fan out from
the runner, with T21 and T23 closing the release.

- **T11 — package scaffold.** 🟢 Landed as `packages/etl` (single `.` export).
  Per the edge recorded in [architecture](architecture.md), the job-model and
  SQL-compilation tiers are `@sisal/core`-only while `runner.ts` alone takes the
  `etl → orm` runtime edge (checkpoint + lock substrate); execution goes through
  the caller-injected `Database`, so unit tests stay network/FFI-free.
  `packages/etl/boundary_test.ts` pins that core/orm/migrate never import
  `@sisal/etl`, that `@sisal/etl` never imports an adapter, a driver, or
  `@sisal/migrate`, and that only the runner tier imports `@sisal/orm`.
- **T12 — `defineJob()`.** The typed job declaration — source table, target
  rollup table, grain (bucket width), window/watermark column, and the transform
  (group keys + filtered aggregates). Types flow from the v0.8 IR and the schema
  snapshot so the target columns and aggregates are checked at definition.
- **T13 — `rollup()` generated SQL.** The extract→aggregate→upsert shape as one
  (or few) pushed-down statements: insert-from-select + date-bucket +
  `FILTER`/`CASE WHEN` aggregates + upsert-from-select keyed on the grain. This
  is the v0.6 PoC promoted to the v0.8 IR. Postgres-first; no row round-trips.
- **T14 — checkpoint store.** Read/advance/prune against the **already-decided**
  `sisal_etl_checkpoints` contract (`job`, `window_end`, `pruned_before`,
  `updated_at`), consuming the v0.9 T13 validation and the v0.9 T14
  prune/`assertReplayable` helpers. v0.10 only consumes ownership; it does not
  re-decide it. 🟢 `read`/`advance` are consumed by `run()`, `assertReplayable`
  by `backfill`/`replay`, and `readState` by `status`. Retention pruning itself
  deliberately stays a substrate call (`etlCheckpoint(db, job).prune(...)` from
  `@sisal/orm`) — v0.10's scope has no prune API of its own, and the
  horizon-first ordering is the substrate's invariant (SEC-012).
- **T15 — window computation.** Compute the next half-open window
  `[from, until)` from the checkpoint watermark + grain, holding the `<= now`
  and bucket-edge invariants the risk register calls out; this is where
  off-by-one double-counting is prevented.
- **T16 — `run(job)`.** The whole single-window runtime: read job → read
  checkpoint → compute window → **acquire the v0.9 portable lock** → send the
  T13 SQL → **advance the checkpoint atomically with the load** (v0.9 T12) →
  exit. Resolves the open question of whether `run()` owns the transaction
  boundary or composes with the caller's, and the multi-statement shape
  (`db.batch` sequence vs one CTE). 🟢 **Resolved: `run()` owns the boundary** —
  the rollup statement and the watermark upsert commit together as one
  `db.batch` (the checkpoint substrate's atomic load+advance); `run()` must not
  be composed into a caller's transaction. The lock is acquired _before_ the
  checkpoint read (never compute a window from a checkpoint another runner may
  still advance); `locked` and `up-to-date` are typed non-run outcomes, not
  errors.
- **T17 — `backfill(job, range)`.** Deterministically re-run an explicit
  historical range as successive windows — same generated SQL, same idempotent
  upsert, no dependence on wall-clock "now."
- **T18 — `replay(job, window)`.** Idempotently re-run a single window; refuse
  windows that begin before the job's `pruned_before` horizon with the v0.9
  `ORM_REPLAY_PRUNED` typed error, with the explicit unsafe override that
  acknowledges re-derivation from a restored source.
- **T19 — `status(job)`.** Report the checkpoint position, retention horizon,
  and last-run state — the read-only observability surface.
- **T20 — dry-run / explain.** Emit the exact generated SQL without executing,
  and pin it with a golden-SQL snapshot per dialect. This is also how the
  acceptance criterion "generated, pushed-down SQL" is verified.
- **T21 — capability-gating.** Postgres is first-class (full pushdown +
  `pg_advisory_lock`). SQLite/libSQL run the rollups they support with the
  lock-row strategy and `FILTER`→`CASE WHEN`; Neon runs via the WebSocket Pool
  favoring one round trip; MySQL is gated by SEC-008 until T1. Unsupported job
  shapes return a typed error — **never a silently-degraded runner.** 🟢 Landed
  as `supportsJob`/`assertJobSupported` (`ETL_UNSUPPORTED_JOB`, the core-only
  tier): an engine allowlist (`postgres`/`sqlite`/`mysql` — the dialects
  carrying the lock + checkpoint substrate; `generic` fails closed) plus a
  **render probe** — the job's rollup is rendered for the exact
  `(dialect, variant, version)` identity so every render-time dialect guard in
  an aggregate refuses **pre-flight**. `run`/`backfill`/`replay` apply the gate
  before the lock, the checkpoint, and the SQL; a refused job sends nothing to
  the driver. With T1 (SEC-008) landed, the MySQL family is enabled rather than
  gated.
- **T22 — external-scheduler docs.** How an external cron / systemd-timer /
  GitHub-Actions / `pg_cron` trigger drives one run; `pg_cron` is optional and
  must never be the only path. 🟢 Landed as
  [docs/etl-scheduling.md](etl-scheduling.md): the runner-script + catch-up loop
  pattern, crontab, systemd timer (`Persistent=true` catch-up), GitHub Actions,
  `Deno.cron` (with the runnable `examples/postgres-family-etl-cron` example —
  schema, seeded events, startup catch-up, hourly cron), and `pg_cron` scoped to
  re-folding a pinned window from `explain()` output, explicitly never the sole
  path. The doc spells out why any cadence is safe: lock-serialized overlaps,
  checkpoint-based misfire catch-up, idempotent re-runs, up-to-date no-ops.
- **T23 — example + integration suite.** A runnable
  `post_events → post_hourly_stats` rollup (Postgres-first), plus the gated
  integration suite modeled on the existing `*-rising-feed` tests: idempotency,
  resume, backfill, and lock-contention (two runners, one wins). 🟢 The runnable
  example is `examples/postgres-family-etl-cron` (landed with T22); the suite is
  `integration/etl_features_test.ts`, gated on `DATABASE_URL` and verified
  against PostgreSQL 16 and 18: dry-run SQL executing verbatim,
  one-window-per-run resume with checkpoint advance, byte-identical replay,
  wipe-and-backfill determinism, deterministic held-lock refusal plus a
  concurrent two-runner race that must not double-count, and the
  `ORM_REPLAY_PRUNED` horizon refusal with the loud unsafe override. All markers
  are fixed instants with `now` injected, so the suite is deterministic. A
  second battery, `integration/etl_limits_test.ts`, deliberately breaks things —
  schema drift, hand-edited watermarks, late-arriving events, clock skew,
  crashed lock holders, pruned sources, 50k-event pushdown,
  grain/identifier/bigint edges — and pins the failure shape and recovery of
  each; the catalogue is [docs/etl-pain-points.md](etl-pain-points.md). The
  battery surfaced and fixed one window-math bug: an unaligned watermark now
  refolds its whole bucket instead of overwriting it with a partial-tail
  undercount.

## Carried-forward — v0.9 [Phase 6](v0.9.0-roadmap.md) deferrals

[v0.9 Phase 6](v0.9.0-roadmap.md) recorded seven items as _explicitly deferred —
record, do not build in v0.9_. They are triaged here. Three align with this
release's **Postgres-first ETL** theme and are scheduled as active v0.10 tasks
(**CF1–CF3**); the other four remain deferred by the release-sequencing
discipline in [roadmap.md](roadmap.md) (npm publish is v0.13+; the rest are
backlog optimizations off the Postgres-first path) and are tracked here only so
nothing is lost (**CF4–CF7**).

| ID  | Task                                                                       | v0.9 origin       | Disposition                                | Effort | Status |
| --- | -------------------------------------------------------------------------- | ----------------- | ------------------------------------------ | ------ | ------ |
| CF1 | `postgres.js` as the default `@sisal/pg` driver (+ upstream `setNoDelay`)  | Phase 6 · perf    | **v0.10** · P1                             | M      | 🟢     |
| CF2 | Materialized-view DDL for Postgres rollup acceleration (investigate→build) | Phase 6 · rollup  | **v0.10** · P2                             | M      | 🟢     |
| CF3 | Recursive-CTE `CYCLE`/`SEARCH` clause (contract 07), portable depth-guard  | Phase 6 · IR      | **v0.10** · P2                             | M      | 🟢     |
| CF4 | MySQL prepared-statement binary-protocol mode (v0.7 B3/B4 opt-in)          | Phase 6 · MySQL   | Backlog (post-v0.10)                       | M      | ⏸️     |
| CF5 | Typed `CREATE FUNCTION` emission (v0.5 item 7 residual)                    | Phase 6 · DDL     | Backlog (post-v0.10)                       | M      | ⏸️     |
| CF6 | Transformable AST over the reserved `SqlChunk.meta` seam (v0.8 item 4)     | Phase 6 · IR      | Dropped (no consumer; DuckDB cut Jul 2026) | L      | ⏸️     |
| CF7 | npm / Node dual publish (blocked on a chosen npm name)                     | Phase 6 · release | Cross-cutting track (demand-driven)        | L      | ⏸️     |

_Status legend adds ⏸️ = recorded, deliberately deferred past v0.10._

**Scheduled into v0.10:**

- **CF1 — `postgres.js` default driver.** v0.10 is Postgres-first and its
  dialect notes _"favor one round trip"_; the current default `jsr:@db/postgres`
  carries a ~40 ms Nagle stall per round trip that directly caps ETL pushdown
  throughput (a prototype swap measured 120 → ~6,800 rps). Make `postgres.js`
  the default while keeping `@db/postgres` selectable, land the upstream
  `setNoDelay(true)` fix, and reconcile the one known divergence (`bigint`
  decoding — now that v0.9 T7 coerces `int8` → string on the pg family, confirm
  parity holds). Re-pin the `@sisal/pg` adapter tests against the new default.
  🟢 Landed: URL connections default to postgres.js (lazily imported);
  `driver: "db-postgres"` keeps the pure-JSR driver selectable
  (`DEFAULT_PG_DRIVER`/`resolvePgDriverKind` pin the default in unit tests).
  Re-verified 49/49 on PostgreSQL 16/17/18 with the new default plus 49/49 on
  the `db-postgres` opt-out (suite wall-clock ~7 s → ~0.6 s); `bigint`
  int8→string parity confirmed on both drivers. The migrate boundary stays on
  `@db/postgres` (short-lived, latency-insensitive, keeps the CLI npm-free). The
  upstream `@db/postgres` `setNoDelay(true)` fix remains an external follow-up
  tracked in `perf/PG_ADAPTER_PERF_REPORT.md` — the default swap removes Sisal's
  exposure either way.
- **CF2 — Materialized-view rollup acceleration.** An alternative to the
  insert-from-select + upsert rollup where Postgres can maintain the aggregate
  itself. Per v0.6/v0.7 discipline this is **investigate first**: does a
  `CREATE MATERIALIZED VIEW` + `REFRESH` path beat the generated-SQL rollup for
  the `post_events → post_hourly_stats` shape, and can it be capability-gated to
  Postgres (SQLite has no equivalent) without a silently-degraded runner? Build
  only if the probe justifies it; otherwise record the decision and defer. 🟢
  **Investigated — recorded defer with benchmark evidence**
  (`perf/pg_matview_probe.ts`, report `perf/PG_MATVIEW_ROLLUP_PROBE.md`,
  PostgreSQL 18.4): steady-state the matview is **7.5–16.5× slower** than the
  incremental window fold (`REFRESH` recomputes all retained history — O(total)
  vs the runner's O(window) — with only 24 h retained; months of history widen
  the gap unboundedly), it cannot express checkpoint/resume/backfill/replay,
  plain `REFRESH` blocks readers, `CONCURRENTLY` needs a unique index and was
  slower still, and it is Postgres-only (a per-engine forked runner — the
  forbidden shape). One recorded counter-point: for a from-scratch full
  recompute `REFRESH` beat the upsert rebuild ~2× — if a whole-history rebuild
  verb is ever added, probe `TRUNCATE` + plain insert-select as an explicit
  maintenance verb. Revisit only if incremental matview maintenance ships in
  core PostgreSQL.
- **CF3 — Recursive-CTE `CYCLE`/`SEARCH`.** The portable depth-guard surface
  shipped in v0.9 (T17); this adds the standard `SEARCH`/`CYCLE` clauses as a
  new construct, capability-gated per engine/version. Not ETL-critical — lands
  as general IR completeness alongside the ETL work. 🟢 Landed:
  `db.$withRecursive(...).searchDepthFirst(by, set)` /
  `.searchBreadthFirst(by, set)` / `.cycle(by, set, using)` — immutable builder
  steps validated at definition time (BY columns must come from the declared
  list; SET/USING names must be fresh; one clause of each kind), rendered after
  the CTE body behind the new `recursiveSearchCycle` capability
  (PostgreSQL-only; SQLite/MySQL families throw typed
  `ORM_DIALECT_UNSUPPORTED`). The set columns are typed on the CTE returned by
  `.as()` but not on `self` (the standard forbids them inside the recursive
  body). Gated by **dialect**, not server version: the pg adapter does not
  populate `dialectIdentity.version`, so a fail-closed `minVersion: "14"` would
  break every live connection — the ≥ 14 server floor is documented instead (all
  verified majors, 16/17/18, qualify). Verified live against a cyclic fixture
  (CYCLE alone terminates a 1→2→3→1 loop, `SEARCH DEPTH FIRST` orders it) on
  pg16/17/18; new `docs/feature-matrix.md` row backed by the
  `recursive search/cycle` scenario.

**Deferred past v0.10 (recorded, not scheduled):** CF4 (MySQL binary protocol)
and CF5 (typed `CREATE FUNCTION`) are backlog optimizations off the
Postgres-first path; CF6 (transformable AST) stays a later non-breaking bump on
the reserved `SqlChunk.meta` seam; CF7 (npm dual publish) is fixed at **v0.13+**
by [roadmap.md](roadmap.md) and blocked on a chosen npm name. Pulling any of
these into v0.10 would violate the release's own non-goals and the documented
sequencing — they are listed so the Phase 6 backlog stays visible, not to commit
them here.

## Goal

Let a developer define a rollup job in TypeScript/Sisal and execute **one**
safe, idempotent run that the database does the heavy lifting for —
extract→group→ aggregate→upsert as generated SQL, with a checkpoint and a lock.

## Package boundary

`@sisal/etl` depends on `@sisal/core` (and an adapter to execute).
**`@sisal/orm` must not depend on `@sisal/etl`.** (See
[architecture](architecture.md).)

## Scope — initial features

- `defineJob()` — declare a job: source, target, grain, window column,
  transform.
- `rollup()` — the grouped extract→aggregate→upsert shape (the v0.6 PoC, now
  typed via the v0.8 IR: insert-from-select + date bucket + filtered
  aggregates + upsert-from-select).
- **checkpoint table** — a watermark store (`job`, `window_end`,
  `pruned_before`, `updated_at`).
- `run(job)` — execute the next window once.
- `backfill(job, range)` — run an explicit historical range deterministically.
- `replay(job, window)` — re-run a specific window idempotently. Both refuse
  windows behind the job's `pruned_before` replay horizon with a typed error
  (the v0.6 A4 replay-vs-retention invariant, test-backed by v0.9); an explicit
  unsafe override acknowledges re-derivation from a restored source.
- `status(job)` — report checkpoint + last-run state.
- **dry-run / explain** — print the generated SQL without executing.
- **SQL-pushdown execution** — the runner sends one (or few) generated
  statements; the database scans/groups/aggregates/upserts. No row round-trips.
- **Postgres-first implementation**; other dialects expose the subset they
  support correctly (per [v0.9](v0.9.0-roadmap.md)).
- **External-scheduler documentation** — how cron/systemd-timer/GitHub-Actions/
  pg_cron triggers a run.

## The execution model (the whole v0.10 runtime)

```
external cron / scheduler wakes up
  → calls the Sisal ETL runner
  → runner reads the job definition
  → runner reads the checkpoint
  → runner computes the next time window  [from, until)
  → runner acquires a lock where supported (pg_advisory_lock; lock-row on SQLite)
  → runner sends generated SQL to the database
  → database scans / groups / aggregates / upserts (set-based, pushdown)
  → runner updates the checkpoint
  → runner exits
```

PostgreSQL may _optionally_ schedule jobs later via `pg_cron`, but the external-
scheduler model is the default — `pg_cron` must never be the only path.

## Non-goals

No full scheduler platform; no worker queue; no Temporal/Airflow clone; no
Rust/native engine; **no DuckDB dependency required** for v0.10.

## Risks

- **Scope creep into a scheduler** — the gravitational pull of "just add cron."
  Held by the non-goals: Sisal defines the job and runs one window.
- **Locking portability** — `pg_advisory_lock` has no SQLite equivalent; the
  v0.9 lock-strategy must be solid or concurrent runs corrupt rollups.
- **Idempotency bugs** — a re-run that double-counts. Upsert keyed on the
  grain + half-open windows `[from, until)` are mandatory invariants.
- **Window boundary errors** — off-by-one at bucket edges (the rising-feed
  examples already show `<= now` discipline).

## Open questions

- Checkpoint table ownership is **answered by v0.6 A3**: default to an
  `@sisal/etl`-managed `sisal_etl_checkpoints` table, unless v0.9 overturns that
  default with evidence and records the resulting dependency edge in
  [architecture](architecture.md). v0.10 consumes the settled boundary; it does
  not decide ownership for the first time.
- ~~Does `run()` own transaction boundaries, or compose with the caller's?~~
  **Answered by T16:** `run()` owns the boundary — one `db.batch` commits the
  rollup and the watermark together; composing into a caller's transaction is
  out of scope for v0.10.
- ~~Multi-statement jobs: a typed `db.batch`-style sequence, or one CTE
  statement?~~ **Answered by T16:** a `db.batch` sequence (the checkpoint
  substrate's `advance(until, load)` shape); a v0.10 job compiles to a single
  rollup statement anyway.

## Acceptance criteria

- `defineJob` + `run` execute the `post_events → post_hourly_stats` rollup on
  PostgreSQL as **generated, pushed-down SQL** (verified by `dry-run` output). —
  **T12, T13, T16, T20**
- A run is **idempotent** (replay a window → identical target state) and
  **resumable** (checkpoint advances; a second run does the next window). —
  **T14, T15, T18**
- `backfill(range)` reproduces a historical range deterministically. — **T17**
- Concurrent runs are serialized by the lock strategy — using the
  **v0.9-tested** portable lock abstraction, not a model first built here. Where
  an engine's ETL lock/checkpoint isn't supported, the API is **capability-gated
  to Postgres-first** and returns a typed "unsupported job shape," never a
  silently-degraded runner. — **T16, T21**
- Checkpoint-table ownership is **already decided** (v0.6 default, v0.9
  validation); v0.10 only consumes it. — **T14**
- `@sisal/orm` has **no** dependency on `@sisal/etl`. — **T11**
- The 0.9.0 security-audit follow-ups (**T1–T10** above) are 🟢 and their
  findings moved to **Addressed** in [docs/security.md](security.md) — or a task
  is explicitly re-triaged there with a rationale. **T1 (SEC-008)** is a hard
  prerequisite of the lock-serialization criterion on the MySQL family; **T5
  (SEC-012)** of the checkpoint/replay criteria.
- The carried-forward v0.9 Phase 6 items scheduled here (**CF1–CF3**) are 🟢 or
  carry a recorded re-triage decision. **CF1 (`postgres.js` default)** must not
  regress the pg adapter's `bigint` behavior or its parity tests; **CF2** must
  land as either a capability-gated implementation _or_ a recorded defer with
  benchmark evidence, never a silently-degraded rollup path. CF4–CF7 remain
  deferred and are **not** release-gating.

## Affected packages

New `@sisal/etl`; depends on `@sisal/core` + an adapter; `@sisal/migrate` if the
checkpoint table is a managed system table. No change to `@sisal/orm`'s surface.

## Required examples / tests

- A runnable `post_events → post_hourly_stats` ETL example (Postgres-first;
  gated integration test like the existing `*-rising-feed` suites).
- Idempotency + resume + backfill + lock-contention tests.
- `dry-run` golden-SQL snapshot per dialect.

## Dialect compatibility notes

PostgreSQL is the first-class target (full pushdown + advisory locks). Neon
works via the WebSocket Pool but the **single-statement / generated-SQL**
preference matters most here — favor one round trip. SQLite/libSQL: rollups run
where insert-from-select + ON CONFLICT are supported, with a lock-row strategy
and `FILTER`→`CASE WHEN`; document any unsupported job shapes rather than
silently degrading.
